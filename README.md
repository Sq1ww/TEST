# Сайт-визитка сантехника

Современный статический сайт-визитка для сантехника с отправкой заявок через Telegram Bot API.

## 🚀 Быстрый старт

### 1. Настройка Telegram бота

1. **Создайте бота через BotFather:**
   - Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
   - Отправьте команду `/newbot`
   - Следуйте инструкциям и получите токен бота (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

2. **Получите Chat ID:**
   - Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
   - Начните с ним диалог — он покажет ваш Chat ID (например: `123456789`)
   - Или создайте группу, добавьте бота и получите Chat ID группы через [@RawDataBot](https://t.me/RawDataBot)

3. **Настройте script.js:**
   - Откройте файл `script.js`
   - Замените `YOUR_BOT_TOKEN` на токен вашего бота
   - Замените `YOUR_CHAT_ID` на ваш Chat ID

```javascript
const TELEGRAM_BOT_TOKEN = '123456789:ABCdefGHIjklMNOpqrsTUVwxyz';
const TELEGRAM_CHAT_ID = '123456789';
```

### 2. Настройка контактов

Откройте `index.html` и обновите контактную информацию:

- **Телефон:** Найдите строку с `+7 (999) 123-45-67` и замените на ваш номер
- **Telegram:** Найдите ссылку `https://t.me/your_telegram` и замените на ваш Telegram

### 3. Деплой на GitHub Pages

#### Вариант A: Через веб-интерфейс GitHub

1. **Создайте репозиторий:**
   - Зайдите на [GitHub](https://github.com)
   - Нажмите "New repository"
   - Назовите репозиторий (например: `plumber-website`)
   - Выберите "Public" (для бесплатного GitHub Pages)
   - Нажмите "Create repository"

2. **Загрузите файлы:**
   - На странице репозитория нажмите "uploading an existing file"
   - Перетащите файлы: `index.html`, `styles.css`, `script.js`
   - Нажмите "Commit changes"

3. **Включите GitHub Pages:**
   - Перейдите в Settings → Pages
   - В разделе "Source" выберите "Deploy from a branch"
   - Выберите ветку `main` и папку `/ (root)`
   - Нажмите "Save"
   - Ваш сайт будет доступен по адресу: `https://ваш-username.github.io/plumber-website/`

#### Вариант B: Через Git (командная строка)

```bash
# Инициализируйте репозиторий
git init

# Добавьте файлы
git add index.html styles.css script.js README.md

# Сделайте первый коммит
git commit -m "Initial commit"

# Добавьте remote репозиторий (замените на ваш URL)
git remote add origin https://github.com/ваш-username/plumber-website.git

# Отправьте код
git branch -M main
git push -u origin main
```

Затем включите GitHub Pages в настройках репозитория (Settings → Pages).

### 4. Настройка собственного домена

1. **Добавьте файл CNAME:**
   - Создайте файл `CNAME` в корне репозитория
   - Добавьте в него ваш домен (например: `plumber.ru`)

2. **Настройте DNS:**
   - Зайдите в панель управления вашего домена
   - Добавьте CNAME запись:
     - **Имя:** `@` или `www`
     - **Значение:** `ваш-username.github.io`
   - Или добавьте A записи (IP адреса GitHub):
     - `185.199.108.153`
     - `185.199.109.153`
     - `185.199.110.153`
     - `185.199.111.153`

3. **Обновите настройки GitHub Pages:**
   - В Settings → Pages добавьте ваш домен в поле "Custom domain"
   - Включите опцию "Enforce HTTPS" (после настройки DNS)

## 📁 Структура проекта

```
.
├── index.html      # Основная HTML страница
├── styles.css      # Стили CSS
├── script.js       # JavaScript для формы и Telegram API
└── README.md       # Инструкция (этот файл)
```

## 🎨 Кастомизация

### Изменение цветов

Откройте `styles.css` и измените переменные в секции `:root`:

```css
:root {
    --primary-color: #2563eb;      /* Основной цвет */
    --primary-dark: #1e40af;       /* Темный оттенок */
    --text-primary: #1f2937;       /* Цвет текста */
    /* ... */
}
```

### Изменение текстов

Все тексты находятся в файле `index.html`. Просто отредактируйте нужные разделы.

## 🔒 Безопасность

⚠️ **ВАЖНО:** Токен Telegram бота хранится в открытом виде в `script.js`. Это нормально для статического сайта, но:

- Не используйте один и тот же бот для разных проектов
- Регулярно проверяйте логи бота на подозрительную активность
- При необходимости создайте отдельного бота для сайта

## 📱 Тестирование

Перед деплоем протестируйте сайт локально:

1. Откройте `index.html` в браузере
2. Проверьте форму заявки (убедитесь, что токен и Chat ID настроены)
3. Проверьте адаптивность на разных устройствах

## 🛠️ Поддержка

Если возникли проблемы:

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что токен бота и Chat ID указаны правильно
3. Проверьте, что бот не заблокирован и может отправлять сообщения

## 📄 Лицензия

Свободное использование. Модифицируйте под свои нужды.

---

**Удачи с вашим сайтом! 🚀**
